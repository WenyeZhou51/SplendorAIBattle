// Dynamic test state - generates a new random state for fresh games
var test_state = new GameState(2);

// Initialize the game with a clean state
test_state.initialize();

console.log("Dynamic test state loaded successfully"); 